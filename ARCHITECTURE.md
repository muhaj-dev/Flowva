# 🏗️ FlowvaHub Rewards System - Architecture

## System Overview

```
┌─────────────────────────────────────────────────────────┐
│                    User Browser                         │
│                                                         │
│  ┌──────────────────────────────────────────────────┐  │
│  │           React App (Vite + TypeScript)          │  │
│  │                                                  │  │
│  │  ┌────────────┐  ┌────────────┐  ┌───────────┐ │  │
│  │  │ Components │  │  Services  │  │  Context  │ │  │
│  │  │   (UI)     │→→│   (API)    │→→│  (State)  │ │  │
│  │  └────────────┘  └────────────┘  └───────────┘ │  │
│  │         ↓              ↓              ↓         │  │
│  │  ┌──────────────────────────────────────────┐  │  │
│  │  │      Supabase Client (@supabase/js)      │  │  │
│  │  └──────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                          ↓ ↑
                    HTTPS / WSS
                          ↓ ↑
┌─────────────────────────────────────────────────────────┐
│                  Supabase Backend                       │
│                                                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │ PostgreSQL   │  │     Auth     │  │   Storage    │ │
│  │   Database   │  │   Service    │  │   (Files)    │ │
│  │              │  │              │  │              │ │
│  │ • Tables     │  │ • JWT Tokens │  │ • Screenshots│ │
│  │ • Functions  │  │ • Sessions   │  │ • Uploads    │ │
│  │ • RLS        │  │ • User Mgmt  │  │ • RLS        │ │
│  └──────────────┘  └──────────────┘  └──────────────┘ │
└─────────────────────────────────────────────────────────┘
```

---

## Frontend Architecture

### Component Hierarchy

```
App.tsx
└── AuthProvider (Context)
    └── RewardsHub (Page)
        ├── Sidebar
        │   ├── Logo
        │   ├── Navigation Menu
        │   └── User Profile
        │
        └── Main Content
            ├── Header
            │   ├── Mobile Menu Button
            │   └── Notifications
            │
            └── Tabs
                ├── Earn Points Tab
                │   ├── Your Rewards Journey
                │   │   ├── PointsBalance
                │   │   ├── DailyStreak
                │   │   └── TopToolSpotlight
                │   │       └── ClaimPointsModal
                │   │
                │   ├── EarnMorePoints
                │   │   ├── Referral Card
                │   │   └── Share Stack Card
                │   │       └── ShareStackModal
                │   │
                │   └── ReferralSection
                │       ├── Stats Display
                │       ├── Referral Link
                │       └── Social Share Buttons
                │
                └── Redeem Rewards Tab
                    └── RewardsGrid
                        ├── Filter Tabs
                        │   ├── All Rewards
                        │   ├── Unlocked
                        │   ├── Locked
                        │   └── Coming Soon
                        │
                        └── Reward Cards
                            └── RewardCard (x8)
```

### Data Flow

```
┌──────────────────────────────────────────────────────┐
│                    User Action                       │
│  (e.g., Click "Check In", "Redeem Reward")          │
└──────────────────────────────────────────────────────┘
                      ↓
┌──────────────────────────────────────────────────────┐
│              Component Event Handler                 │
│  e.g., handleCheckIn(), handleRedeem()              │
└──────────────────────────────────────────────────────┘
                      ↓
┌──────────────────────────────────────────────────────┐
│              Service Layer Function                  │
│  userService.dailyCheckIn(userId)                   │
│  rewardsService.redeemReward(userId, rewardId)      │
└──────────────────────────────────────────────────────┘
                      ↓
┌──────────────────────────────────────────────────────┐
│           Supabase RPC / Database Query              │
│  supabase.rpc('handle_daily_checkin', ...)          │
└──────────────────────────────────────────────────────┘
                      ↓
┌──────────────────────────────────────────────────────┐
│           PostgreSQL Function Execution              │
│  • Validate conditions                              │
│  • Update database                                  │
│  • Log activity                                     │
│  • Return result                                    │
└──────────────────────────────────────────────────────┘
                      ↓
┌──────────────────────────────────────────────────────┐
│              Response to Frontend                    │
│  { success: true, points: 5 }                       │
└──────────────────────────────────────────────────────┘
                      ↓
┌──────────────────────────────────────────────────────┐
│              Update UI State                         │
│  refreshUserProfile() → Re-fetch user data          │
│  Component re-renders with new data                 │
└──────────────────────────────────────────────────────┘
```

---

## Database Architecture

### Entity Relationship Diagram

```
┌─────────────────┐
│   auth.users    │ (Supabase Auth)
│─────────────────│
│ id (PK)         │
│ email           │
│ password_hash   │
└─────────────────┘
        │
        │ 1:1
        ↓
┌─────────────────┐
│  public.users   │ (User Profiles)
│─────────────────│
│ id (PK, FK)     │───┐
│ email           │   │
│ points_balance  │   │ 1:N
│ daily_streak    │   │
│ last_check_in   │   │
│ referral_code   │   ↓
│ referred_by (FK)│  ┌──────────────────┐
└─────────────────┘  │ user_activities  │
        │            │──────────────────│
        │ 1:N        │ id (PK)          │
        │            │ user_id (FK)     │
        ↓            │ activity_type    │
┌─────────────────┐  │ points_earned    │
│  user_rewards   │  │ points_spent     │
│─────────────────│  │ description      │
│ id (PK)         │  │ created_at       │
│ user_id (FK)    │  └──────────────────┘
│ reward_id (FK)  │
│ points_spent    │         ↑
│ status          │         │ 1:N
│ redeemed_at     │         │
└─────────────────┘  ┌─────────────────┐
        │            │  public.users   │
        │ N:1        └─────────────────┘
        ↓                    │
┌─────────────────┐         │ 1:N
│    rewards      │         ↓
│─────────────────│  ┌─────────────────┐
│ id (PK)         │  │   user_tasks    │
│ name            │  │─────────────────│
│ description     │  │ id (PK)         │
│ point_cost      │  │ user_id (FK)    │
│ category        │  │ task_id (FK)    │
│ status          │  │ submission_data │
│ emoji           │  │ status          │
│ order_index     │  │ verified_at     │
└─────────────────┘  │ points_awarded  │
                     └─────────────────┘
                             │ N:1
┌─────────────────┐         ↓
│   referrals     │  ┌─────────────────┐
│─────────────────│  │     tasks       │
│ id (PK)         │  │─────────────────│
│ referrer_id(FK) │  │ id (PK)         │
│ referee_id (FK) │  │ name            │
│ status          │  │ description     │
│ points_awarded  │  │ points_reward   │
│ completed_at    │  │ requirements    │
└─────────────────┘  │ status          │
                     │ external_link   │
                     └─────────────────┘
```

### Key Database Functions

```
┌─────────────────────────────────────────┐
│  handle_daily_checkin(user_id)         │
├─────────────────────────────────────────┤
│ 1. Get user's last_check_in date       │
│ 2. If today = already checked in       │
│    → Return 0 points                   │
│ 3. If yesterday = continue streak      │
│    → streak + 1                        │
│ 4. If older = reset streak             │
│    → streak = 1                        │
│ 5. Update user: +5 points, new streak  │
│ 6. Log activity                        │
│ 7. Return 5 points                     │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│  redeem_reward(user_id, reward_id)     │
├─────────────────────────────────────────┤
│ 1. Check if reward is unlocked         │
│ 2. Check if user has enough points     │
│ 3. Check if already redeemed           │
│ 4. Deduct points from user             │
│ 5. Create redemption record            │
│ 6. Log activity                        │
│ 7. Return success/error                │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│  complete_referral(referee_id)         │
├─────────────────────────────────────────┤
│ 1. Get referrer from referee record    │
│ 2. Check if already completed          │
│ 3. Award +25 points to referrer        │
│ 4. Update referral status              │
│ 5. Log activity                        │
│ 6. Return success                      │
└─────────────────────────────────────────┘
```

---

## Security Architecture

### Row Level Security (RLS) Flow

```
┌────────────────────────────────────────────────┐
│         User Makes Database Request            │
│  supabase.from('users').select(...)           │
└────────────────────────────────────────────────┘
                    ↓
┌────────────────────────────────────────────────┐
│         Supabase Validates JWT Token           │
│  Extracts: auth.uid(), auth.email(), etc.     │
└────────────────────────────────────────────────┘
                    ↓
┌────────────────────────────────────────────────┐
│         RLS Policy Evaluated                   │
│  POLICY: Users can view own profile            │
│  USING (auth.uid() = id)                      │
└────────────────────────────────────────────────┘
                    ↓
              ┌─────┴─────┐
              │           │
          PASS │         │ FAIL
              ↓           ↓
    ┌──────────────┐  ┌──────────────┐
    │ Return Data  │  │ Return Empty │
    │ to User      │  │ or Error     │
    └──────────────┘  └──────────────┘
```

### Security Layers

1. **Authentication**: Supabase Auth (JWT tokens)
2. **Row Level Security**: PostgreSQL RLS policies
3. **Function Security**: `SECURITY DEFINER` for trusted operations
4. **Storage Security**: Bucket-level + path-based policies
5. **API Keys**: Environment variables (never in code)

---

## File Storage Architecture

```
Storage Bucket: task-submissions (Private)

Structure:
task-submissions/
├── {user_id_1}/
│   ├── {task_id_1}/
│   │   ├── 1234567890.png
│   │   └── 1234567891.jpg
│   └── {task_id_2}/
│       └── 1234567892.png
└── {user_id_2}/
    └── {task_id_1}/
        └── 1234567893.png

RLS Policies:
• Users can upload to: {bucket}/{their_user_id}/*
• Users can read from: {bucket}/{their_user_id}/*
• Admins can read all (future feature)
```

---

## State Management

```
┌─────────────────────────────────────────────┐
│          AuthContext (Global State)         │
│─────────────────────────────────────────────│
│ • user: SupabaseUser | null                │
│ • userProfile: User | null                 │
│ • loading: boolean                         │
│ • signIn(email, password)                  │
│ • signUp(email, password, referralCode?)   │
│ • signOut()                                │
│ • refreshUserProfile()                     │
└─────────────────────────────────────────────┘
              ↓ Provides to all children
┌─────────────────────────────────────────────┐
│           All Components Can Access         │
│  const { user, userProfile } = useAuth()   │
└─────────────────────────────────────────────┘

Component Local State:
┌─────────────────────────────────────────────┐
│         RewardsHub Component                │
│─────────────────────────────────────────────│
│ • canCheckIn: boolean                      │
│ • checkingIn: boolean                      │
│ • referralStats: { total, points }         │
│ • isMobileSidebarOpen: boolean             │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│         RewardsGrid Component               │
│─────────────────────────────────────────────│
│ • rewards: Reward[]                        │
│ • filteredRewards: Reward[]                │
│ • filter: RewardFilter                     │
│ • loading: boolean                         │
│ • redeeming: boolean                       │
└─────────────────────────────────────────────┘
```

---

## API Service Layer

```
Services Architecture:

┌──────────────────┐
│  authService     │ → Sign up, sign in, sign out
├──────────────────┤
│ • signUp()       │
│ • signIn()       │
│ • signOut()      │
│ • getSession()   │
└──────────────────┘

┌──────────────────┐
│  userService     │ → User profile, points, streaks
├──────────────────┤
│ • getUserProfile()      │
│ • updateUserProfile()   │
│ • dailyCheckIn()        │
│ • canCheckInToday()     │
│ • getReferralStats()    │
│ • getReferralLink()     │
└──────────────────┘

┌──────────────────┐
│ rewardsService   │ → Rewards catalog, redemption
├──────────────────┤
│ • getAllRewards()       │
│ • getRewardsByFilter()  │
│ • redeemReward()        │
│ • getUserRewards()      │
│ • getRewardCounts()     │
└──────────────────┘

┌──────────────────┐
│  tasksService    │ → Task submission, verification
├──────────────────┤
│ • getActiveTasks()      │
│ • getUserTasks()        │
│ • submitTask()          │
│ • uploadScreenshot()    │
│ • hasSubmittedTask()    │
└──────────────────┘
```

---

## Deployment Architecture

### Production Setup

```
┌─────────────────────────────────────────────┐
│              Vercel / Netlify               │
│  (Static hosting for React app)            │
│                                             │
│  • Build: npm run build                    │
│  • Output: dist/                           │
│  • Environment: VITE_SUPABASE_*            │
└─────────────────────────────────────────────┘
                    ↓ ↑
                HTTPS API Calls
                    ↓ ↑
┌─────────────────────────────────────────────┐
│            Supabase Cloud                   │
│  (Managed PostgreSQL + Auth + Storage)     │
│                                             │
│  • Database: PostgreSQL 15                 │
│  • Auth: GoTrue                            │
│  • Storage: S3-compatible                  │
│  • Region: Auto-selected                   │
│  • Backups: Automatic                      │
└─────────────────────────────────────────────┘
```

---

## Performance Optimizations

### Frontend
- ✅ Code splitting (React lazy loading ready)
- ✅ Tailwind CSS purging (production builds)
- ✅ Image optimization (emoji instead of images)
- ✅ Minimal dependencies
- ✅ React memo for expensive components (future)

### Backend
- ✅ Database indexes on foreign keys
- ✅ RLS policies for security + performance
- ✅ PostgreSQL functions for complex queries
- ✅ Efficient SELECT queries (only needed columns)

### Network
- ✅ Supabase global CDN
- ✅ HTTPS/2 support
- ✅ Gzip compression
- ✅ JWT token caching

---

## Monitoring & Debugging

### Available Logs

```
Frontend Console:
• Component render logs
• API call errors
• State changes

Supabase Dashboard:
• API logs (Settings → API)
• Database logs (Database → Logs)
• Auth logs (Authentication → Logs)
• Storage logs (Storage → Logs)

SQL Queries:
• Check query performance in Logs
• View RLS policy evaluation
• Monitor function execution
```

---

This architecture is designed to be:
- **Scalable**: Handles thousands of users
- **Secure**: Multi-layer security
- **Maintainable**: Clean separation of concerns
- **Extensible**: Easy to add new features
- **Production-ready**: Deployed in minutes
